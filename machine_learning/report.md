機械学習プロジェクト課題レポート
基本情報
学生証番号：24RB1301
氏名：長谷川樹
割り当てられたデータセット：B

列ごとに正規化、全部に欠損値がないか、

1. 使用した最終モデル

Gradient Boosting

2. 前処理

標準化（StandardScaler）

3. モデルの選択方法

8種類のモデルに対して5分割交差検証を行い、balanced accuracyを比較した。最も高いスコアを示したモデルを最終モデルとして選択した。

4. 比較したモデル

| モデル | 主な設定 | 検証時のbalanced accuracy |
|---|---|---|
| ロジスティック回帰 | デフォルト | 0.6697 |
| ロジスティック回帰（L1正則化） | l1_ratio=1, solver=saga | 0.6685 |
| 線形判別分析 | デフォルト | 0.6698 |
| Naive Bayes | デフォルト | 0.7013 |
| SVM | kernel=rbf | 0.7243 |
| 決定木 | デフォルト | 0.7333 |
| ランダムフォレスト | デフォルト | 0.8006 |
| Gradient Boosting | デフォルト | **0.8594** |

5. 最終モデルを選んだ理由

5分割交差検証によるbalanced accuracyが0.8594と全モデルの中で最も高く、標準偏差も0.0178と安定していたため。

6. 過学習を防ぐための工夫

5分割交差検証を用いてモデルを評価した。訓練データだけの性能ではなく、交差検証スコアに基づいてモデルを選択することで、未知データへの汎化性能を重視した。

7. データの特徴についての考察

線形モデル（ロジスティック回帰、LDA）のスコアが0.67前後と低く、Gradient Boostingやランダムフォレストなどのアンサンブルモデルが高い精度を示したことから、データセットBの特徴量間には非線形な関係が含まれており、決定木ベースのモデルが有効なデータであったと考えられる。
